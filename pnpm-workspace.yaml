# pnpm workspace — root deps + first-party @agent-cloud/* / agent-cloud-wasm.
#
# `packages` is required: pnpm errors with "packages field missing or empty"
# when the field is absent (CI `pnpm install` for Hive/Playwright).
#
# Overrides / build-policy live in package.json#pnpm so pnpm 9 (CI) and
# pnpm 10 (local) share one config surface.
packages:
  - "."
  - "clients/mobile-lovable"
  - "packages/*"
  - "proto"
