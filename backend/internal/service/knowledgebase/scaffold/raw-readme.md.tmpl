# Raw sources

Immutable ground truth for "{{.Name}}". Drop source documents here (or let a
connector sync them in under a subdirectory). Agents read this directory but
never modify it — the wiki under `wiki/` is recompiled from these files.
