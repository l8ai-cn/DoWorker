# Hive 复验报告 — 2026-07-05 10:15 (UTC+8)

环境：backend :10015 · web :10007 · web-user :10020 · runner-e2e-echo 容器 · postgres 健康

## 执行摘要

| 核验项 | 方法 | 结果 |
|--------|------|------|
| API 全量 S0–S3 | `bash deploy/dev/hive_smoke.sh` | **PASS**（~29s） |
| 连续重跑幂等 | 紧接再跑一轮 hive_smoke | **PASS** |
| V1b 负面测试 | 注入 version=999 → smoke fail-fast → 恢复 | **PASS**（打印 `count\|dirty = 2\|f` 后退出） |
| 迁移表 | psql 实查 | `1\|f\|166` |
| usage 双路径 | pod_session_usage 最近行 | gpt-4o + gpt-4o-mini 并存 |
| deny 方向 | S3.2 `verdict direction is deny` | **PASS** |
| 浏览器闭环 | chromium `hive-browser-integration.mjs` | **PASS**（session=conv_b6a403f2c8184857，items=2） |

## 原始输出摘录

### hive_smoke（S3 关键步）

```
✓ S3.3 usage_by_model from agent report — gpt-4o
✓ S3.2 runner auto-denied (no elicitation) — items=2
✓ S3.2 verdict direction is deny — assistant text confirms deny
hive smoke: all suites passed
```

### 浏览器

```
✓ Web User: send message in browser — session=conv_b6a403f2c8184857
✓ API: conversation items persisted — items=2
```

### DB

```
schema_migrations: 1|f|166
pod_session_usage (recent): gpt-4o + gpt-4o-mini rows
```

## 本轮修复

- `hive-browser-integration.mjs`：支持 `WEB_USER_URL` / `WEB_USER_AUTH_URL` 环境变量；
  web-user JWT key 必须用 `http://localhost:10000`（与 Vite 默认一致），不能用 `127.0.0.1`。

## 环境备注

- `bazel run //deploy/dev:backend_only` 因 Docker Hub 拉取 `node:20-bookworm-slim` 失败
  （registry EOF），但已有 postgres + runner 容器 + host ibazel backend 可正常工作。
- web-user 需单独 `npm run dev -- --port 10020`（lifecycle 脚本依赖未 source 的 helper）。

## 判定

**P1 全部通过，可采信。** P4 延伸项（Claude 实时 usage、cost_budget、read-state 落库等）仍未实现，属计划外。
