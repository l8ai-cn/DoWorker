apiVersion: batch/v1
kind: Job
metadata:
  name: minio-setup
  namespace: agentsmesh
spec:
  backoffLimit: 10
  ttlSecondsAfterFinished: 300
  template:
    metadata:
      labels:
        app: minio-setup
    spec:
      restartPolicy: OnFailure
      imagePullSecrets:
        - name: agentsmesh-regcred
      containers:
        - name: mc
          image: repo.aiedulab.cn:8443/agentsmesh/mc:latest
          imagePullPolicy: IfNotPresent
          env:
            - name: MINIO_ROOT_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: agentsmesh-secrets
                  key: MINIO_ROOT_PASSWORD
          command:
            - sh
            - -c
            - |
              set -e
              until mc alias set am http://minio:9000 minioadmin "$MINIO_ROOT_PASSWORD"; do
                echo "waiting for minio..."; sleep 3
              done
              mc mb --ignore-existing am/agentsmesh
              echo "bucket ready"
